apiVersion: apps/v1
kind: Deployment
metadata:
  name: new-calendar-application
  namespace: new-calender
spec:
  replicas: 1
  selector:
    matchLabels:
      app: new-calendar-application
  template:
    metadata:
      labels:
        app: new-calendar-application
    spec:
      containers:
        - name: new-calendar-application
          image: bahavathyacr.azurecr.io/new-calender-app:latest
          ports:
            - containerPort: 80
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__DefaultConnection
              value: "/app/data/appointments.db"   # SQLite path example
          volumeMounts:
            - name: db-volume
              mountPath: /app/data
      volumes:
        - name: db-volume
          emptyDir: {}   # NOTE: AKS pods will lose DB on restart (SQLite not recommended)

---
apiVersion: v1
kind: Service
metadata:
  name: new-calendar-service
  namespace: new-calender
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: new-calendar-application

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: new-calendar-ingress
  namespace: new-calender
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: calendar.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: new-calendar-service
                port:
                  number: 80
